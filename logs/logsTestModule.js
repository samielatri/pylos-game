const { logThis } = require('./pylosGame');

const object1 = {
  a: 'somestring',
  b: 42,
  c: false
};

logThis(object1);
logThis('And all the roads we have to walk are winding, And all the lights that lead us there are blinding');
